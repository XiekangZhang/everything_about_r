# everything_about_r

install.packages("package name")
remove.packages("package name")
library("loading package")
detach("loaded package")

x <- value / x = value

everything in R is vector. A vector is a collection of elements, all of the same type
A dataframe can contain many different data type in each column.
